let requestBodyjsonData = try! JSONEncoder().encode( {{ op.request.objectName }})
let requestBodyData: Data? =  String(data: requestBodyjsonData, encoding: .utf8)!.data(using: .utf8)

// Construct request
guard let requestUrl = url.appendingQueryParameters(queryParams) else { return }
guard let request = try? HTTPRequest(method: .{{ op.request.method }}, url: requestUrl, headers: headers, data: requestBodyData) else { return }
