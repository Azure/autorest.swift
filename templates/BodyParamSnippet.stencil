{% if op.params.body.strategy == "plain" or op.params.body.strategy == "unixTime" or op.params.body.strategy == "plainNullable" or op.params.body.strategy == "byteArray" %}
    {{ op.params.body.param.name }}: {{ op.params.body.param.type }},
{% endif %}
