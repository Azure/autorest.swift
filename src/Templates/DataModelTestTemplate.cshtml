@using AutoRest.Swift
@using AutoRest.Core.Model
@using AutoRest.Swift.Model
@using AutoRest.Swift.Templates
@using AutoRest.Core.Utilities

@inherits AutoRest.Core.Template<AutoRest.Swift.Model.CompositeTypeSwift>
import XCTest
import Foundation
@@testable import @(CompositeTypeSwift.TestNamespace)

class @(Model.Name)Test : XCTestCase {
    func testModel() {
        var model = @(Model.Name)()
        @(Model.FieldWriter.FieldsForTest())

        let encoder = JSONEncoder()
        do {
            let jsonData = try encoder.encode(model)
            print(jsonData)
        } catch _ {
            print("Error");
        }
    }
}
